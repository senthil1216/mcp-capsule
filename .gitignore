# Capsule runtime artifacts
audit.log
demo/fake_github_events.jsonl
demo/taint_store.jsonl
demo/pending_approvals.jsonl

# Generated demo recordings (regenerate with `make demo-gif`)
docs/capsule-demo.gif
docs/demo.cast

# Draft content kept local (not published in-repo)
docs/linkedin-teaser.md

# Honeytokens / disposable env — never commit real-looking secrets
bench/.honeytokens/
*.honeytoken

# Ephemeral sandbox run dirs
.capsule/
.sandbox-runs/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/
build/
dist/
